# OXXYEN SOCIAL

Полностью рабочий, самодостаточный социальный мессенджер с фокусом на приватность и скорость.

## Особенности

- 🔐 Полная приватность: данные шифруются на клиенте, нет трекеров
- 💬 Реал-тайм чаты: личные и групповые с историей сообщений
- 📢 Лента постов: хронологическая, без алгоритмов
- 🛡️ Безопасность: защита от XSS, CSRF, шифрование файлов
- 🌙 Темная тема по умолчанию с возможностью переключения
- 🚀 Локальный запуск с публичным доступом через ngrok

## Требования

- Node.js 20+
- MongoDB (локально или Atlas)
- ngrok (для публичного доступа)

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd oxxyen-social
   ```

2. Установите зависимости:
   ```bash
   cd backend && npm install
   cd ../frontend && npm install
   ```

3. Настройте переменные окружения:
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашими настройками
   ```

4. Запустите MongoDB (если локально):
   ```bash
   mongod --dbpath /path/to/your/db
   ```

## Запуск

Запустите всё одной командой:
```bash
./backend/start.sh
```

Или вручную:
```bash
# Терминал 1: Backend
cd backend && npm run dev

# Терминал 2: Frontend
cd frontend && npm run dev

# Терминал 3: Ngrok
ngrok http 3000
```

Приложение будет доступно по адресу ngrok (например, https://abc123.ngrok.io)

## Регистрация

1. Откройте приложение в браузере
2. Нажмите "Регистрация"
3. Введите email, пароль и уникальный username
4. Подтвердите email (для восстановления пароля)

Тестовый аккаунт: test@oxxyen.social / password123

## Использование

### Профиль
- Загрузите аватар (PNG/JPG до 5MB)
- Установите описание и статус
- Настройте приватность

### Чаты
- Создайте личный чат или группу
- Отправляйте сообщения, эмодзи, изображения
- Прикрепляйте файлы (кроме .exe, .apk, .dll)

### Лента
- Публикуйте посты с текстом, фото, видео
- Лайкайте, комментируйте, репостите
- Подписывайтесь на пользователей (с подтверждением)

## Безопасность

- Пароли хэшируются с bcrypt
- JWT токены для сессий
- 2FA через TOTP
- Клиентское шифрование файлов AES-256-GCM
- Валидация всех входов
- Логи доступа только для администратора

## API

REST API доступен на /api/v1/
WebSocket для реал-тайм чатов на /

## Разработка

- Backend: Node.js + Express + Socket.IO
- Frontend: React + Vite + Tailwind CSS
- Database: MongoDB с Mongoose
- State: Zustand

## Лицензия

MIT
